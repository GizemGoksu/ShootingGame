import 'package:flutter/material.dart';

class AppConst {
  static List<int> targetAreaPoints = [6, 7, 8, 9, 10];
  static int headShotPoint = 12;
  static const Duration normalSpeedDuration = Duration(seconds: 3);
  static const Duration highSpeedDuration= Duration(seconds: 1);
  static const double aimRadius = 84;
  static String totalPoint = 'Total Points';
}
