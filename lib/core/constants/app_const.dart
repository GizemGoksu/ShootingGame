import 'package:flutter/material.dart';

class AppConst {
  static List<int> targetAreaPoints = [6, 7, 8, 9, 10];
  static int headShotPoint = 12;
  static const Duration animationDuration = Duration(seconds: 3);
  static const Duration shorDuration = Duration(seconds: 1);

}
